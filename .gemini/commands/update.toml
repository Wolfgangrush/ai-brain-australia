description = "🔄 Update the firm code, skills, and prompts from upstream. Wraps `pip install --upgrade git+<repo>`. Your matter data + your project CLAUDE.md are NEVER touched — only firm code/skills/prompts get refreshed. Run this monthly or when the publisher tells you a new version is out."

prompt = """
# 🔄 Update — Australia AI Brain

The advocate said:

\"\"\"
{{args}}
\"\"\"

You are updating the Australia AI Brain to its latest published version.

## Step 1 — Confirm the advocate's intent

Tell the advocate:

> 🔄 I'm about to run:
>
>     ailawfirm-australia update
>
> This pulls the latest firm code, skills, and prompts from the upstream repository:
> https://github.com/Wolfgangrush/ai-law-firm-australia
>
> Your matter data + your project root CLAUDE.md will NOT be touched. Only the firm's internal code/skills/prompts get refreshed.
>
> Proceed? (yes / no)

If the advocate says no — STOP. Do nothing.

## Step 2 — Run the update

!{ailawfirm-australia update}

## Step 3 — Report the result

If exit code is 0:

> ✅ Firm updated. To see what's new, run:
>
>     ailawfirm-australia status

If non-zero:

> ❌ Update failed. Try the manual command:
>
>     pip install --upgrade git+https://github.com/Wolfgangrush/ai-law-firm-australia.git
>
> Common causes: no internet, pip permissions, virtual environment not activated.

## Step 4 — Suggest restart

After a successful update, tell the advocate to close any open `ailawfirm-australia` sessions and re-launch so the new skills + prompts load.
"""
